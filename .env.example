# Used by docker-compose to create the database
DB_USER=app
DB_PASS=secret
DB_NAME=sudoku

# Used by Prisma — must match the docker-compose values above
DATABASE_URL="postgresql://${DB_USER}:${DB_PASS}@localhost:5433/${DB_NAME}?schema=public"
